apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-deployment
  namespace: fashion-marketplace
  labels:
    app: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: fashion-marketplace
spec:
  ports:
    - port: 6379
  selector:
    app: redis
